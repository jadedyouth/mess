/*
void checkPots () {
leds[298] = CHSV(hue, sat, val);
leds[297] = CHSV(hue1, sat1, val1);
  LEDS.show();

if (cl == 0) {

if (df == 1) { //default or changes  - switches off pots

if (lr ==0) {
hue = analogRead(A6) /4 ; 
sat = analogRead(A7) / 4;
val = analogRead(A2) / 4;
del = analogRead(A1);
pil = analogRead(A0) / 4;    

}
if (lr ==1) {
hue1 = analogRead(A6) / 4; 
sat1 = analogRead(A7) / 4; 
val1 = analogRead(A2) / 4; 
del1 = analogRead(A1);
pil1 = analogRead(A0) / 4;     
}

}

else if (df == 0){
hue = dhue;
sat = dsat;
val = dval;
del = ddel;
pil = dpil;
hue1 = dhue1;
sat1 = dsat1;
val1 = dval1;
del1 = ddel1;
pil1 = dpil1;

whue = dhue;
wsat = dsat;
wval = dval;
wdel = ddel;
wpil = dpil;
whue1 = dhue1;
wsat1 = dsat1;
wval1 = dval1;
wdel1 = ddel1;
wpil1 = dpil1;
  
}

}
else if (cl == 1)  {
  
wmd = md;
whue = analogRead(A6) /4 ; 
wsat = analogRead(A7) / 4;
wval = analogRead(A2) / 4;
wdel = analogRead(A1) * 5;
wpil = analogRead(A0) / 4;  
whue1 = analogRead(A6) / 4; 
wsat1 = analogRead(A7) / 4; 
wval1 = analogRead(A2) / 4; 
wdel1 = analogRead(A1) * 5;
wpil1 = analogRead(A0) / 4; 
}
}

*/
